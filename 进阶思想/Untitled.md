# Json schema

用json来定义json应该长什么样，即对数据的数据结构的描述



# babel

@babel/parser——将代码转为AST

@babel/traverse——对AST进行操作

@babel/generator——将AST转为代码文件



# 事件

message事件

触发时机：窗口接收到来自其他窗口/iframe 的 postMessage() 消息时触发

核心用途：跨窗口/跨域通信（如父页面与 iframe 之间通信）

