# 背景

由于`vite8`之前开发环境（按需打包）与生产环境（全量打包），所以存在开发环境与线上环境打包产物表现不一致的风险。尤其是css样式不一致的问题容易发生，需求开发过程中，已有多次本地开发环境css样式符合UI，而线上环境出现差异的场景



# 与Vite效率对比

打包时长、流水线构建时长减少

冷启动时，首屏渲染时间变长，但是页面切换时间变短。（因为Rsbuild是全量打包，而vite是根据页面按需打包，而全量打包是目前解决开发和生产产物不一致的必要条件，所以首屏渲染时间变长而页面切换时间变短）

热更新时长相当

分包数css、js减少